# homebrew-tap

Homebrew formulae for [purge](https://github.com/soummyaanon/purge) — reclaim
the regenerable junk on your Mac.

```
brew install soummyaanon/tap/purge
purge
```

`brew upgrade purge` picks up new releases. The formula is bumped
automatically by purge's release workflow.
